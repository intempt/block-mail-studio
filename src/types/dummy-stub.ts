
// @ts-nocheck
// Complete stub file to replace all dummy imports

export const userAttributes: any = [];
export const users: any = [];
export const userDetails: any = {};
export const variables: any = [];

export default {
  userAttributes,
  users,
  userDetails,
  variables
};

// Export everything as any to avoid type issues
export * from './userAttributes';

// Function stubs
export const getUserDetails = (): any => ({});
export const getUser = (): any => ({});
export const getVariable = (): any => ({});
